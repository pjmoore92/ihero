{% extends "base.html" %}

{% block title %}Home{% endblock %}

{% block content %}
<div class="hero-unit">
  <input type="text" style="text-align:center" class="span5  search-query" placeholder="Search">
  <h2 style="text-align:center">Incident List</h2>
</div>

<div class="accordion" id="accordion2">
  {% if latest_incidents %}
  {% for i in latest_incidents %}
  <div class="accordion-group">
    <div class="accordion-heading">
      <a
	 class="accordion-toggle"
	 data-toggle="collapse"
	 data-parent="#accordion2"
	 href="#"
	 >
	{{i.title}}<i>- Click to Expand</i>
      </a>
    </div>
    <div id="{{i.id}}" class="accordion-body collapse">
      <div class="accordion-inner">
	<div class="well">
	  {{i.description}}
	</div><!-- end .well -->
      </div>
    </div>
  </div>
  {% endfor %}
  {% else %}
  <div>
    <p>No Incidents Available</p>
  </div>   
  {% endif %}
  {% endblock %}
